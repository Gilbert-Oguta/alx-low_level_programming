#include "main.h"
/**
 * _strcpy - copy paste string
 * @dest: destination
 * @src: source
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	int num = 0;

	while (*(src + num) != '\0'
			{
			*(dest + num) = *(src + num);
			num++;
			}
			*(dest + num) = '\0';
			return (dest);
}

