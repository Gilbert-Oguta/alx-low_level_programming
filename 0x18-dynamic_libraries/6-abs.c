#include "main.h"
/**
 *  _abs - computes the absolute value of an integer
 *  @c: The int in question
 *  Return: The abs of the init
 */

int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (-c);
}
